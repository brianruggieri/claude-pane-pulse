{
  "name": "test-project",
  "version": "1.0.0",
  "description": "Small demo project for testing claude-pane-pulse title monitoring",
  "scripts": {
    "test": "node --test tests/math.test.js tests/auth.test.js",
    "lint": "echo 'Linting...' && node -e \"require('./src/math'); require('./src/auth'); console.log('No lint errors.')\""
  },
  "private": true
}
